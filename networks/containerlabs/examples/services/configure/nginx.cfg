server {
    listen 80;
    server_name localhost;

    location / {
        root /var/www/localhost/htdocs;
        index index.html;
    }

    location /admin {
        root /var/www/localhost/htdocs;
        index admin.html;
    }
}